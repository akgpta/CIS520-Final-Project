% cartesian.m

% find all permuations of given array 
function [res] = cartesian(A, d);
m = size(A)

res = zeros(m^d, d);

for i = 0:m^d - 1
    curr = ones(d,1)
    for j = 1:d
    res(i+1,:) = 1.+[mod(i,d) mod(i,d^2) - mod(i,d)
    
end


% A is m x 1
% d is 1 x 1
% res is   m^d x d



end